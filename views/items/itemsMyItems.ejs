<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', { title: 'My Items' }); %>
<body>
  <%- include('../partials/header', { currentUser: currentUser }) %>
  
  <h1>My Items</h1>

  <ul>
    <% usersItems.forEach((item) => { %>
      <%- include('../partials/itemCard', { item: item }); %>
    <% }); %>
  </ul>
</body>
</html>